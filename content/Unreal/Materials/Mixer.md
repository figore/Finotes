  * # Sync texture to UE
    * create a folder in UE content called mixer
    * export everything to that folder
    * go to the "MS Presets"
    * search for the "M_MS_Default_Material," select "Duplicate,"
    * move it to mixer
    * create material instance