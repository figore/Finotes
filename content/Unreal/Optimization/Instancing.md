  * ## Instanced static mesh is crated in blueprint with + sign
    * it will duplicate exactly the same piece with 1 draw call but it will also connect the LOD
    * if one LOD is turned on then all of them are turned on
  * Hierarchical static mesh is needed for huge landcapes and trees where different LODs are important at the cost of performance